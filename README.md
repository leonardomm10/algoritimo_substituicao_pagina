# algoritimo_substituicao_pagina
Algoritimo PHP que faz a simulação de troca de página utilizando LRU, FIFO, LIFO e OTIMO

Requisitos:

- Docker
- Docker compose

Executar o Docker

- docker-compose -f docker/docker-compose.yml up -d